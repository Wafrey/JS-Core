function solve(arr) {
    let [input, town, time, flightNum, gateNum] = arr;

    console.log(`${input}: Destination - ${town}, Flight - ${flightNum}, Time - ${time}, Gate - ${gateNum}`);

}

solve(['Departures', 'London', '22:45', 'BR117', '42']);
